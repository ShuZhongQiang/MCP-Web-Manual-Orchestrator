# IDE Configs

本目录用于集中管理多 IDE 的 MCP 接入配置与使用说明。

当前提供：
- `claude-desktop/`：Claude Desktop 配置示例
- `cursor/`：Cursor 配置示例
- `trae/`：Trae 配置示例与 Skill 约束文件

通用前置条件：
- 已执行 `npm install`
- 已执行 `npm run build`
- MCP Server 启动命令为 `node dist/index.js`
